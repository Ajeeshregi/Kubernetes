apiVersion: v1
kind: Pod
metadata:
  name: mysqlpod-new
  namespace: ajeeshregi
  labels:
    env: dev
    user: ajeesh
spec:
  containers:
  - name: c1
    image: mysql
    envFrom:
    - configMapRef:
        name: cm1